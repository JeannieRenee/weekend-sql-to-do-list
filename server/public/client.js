// Linked Successfully
console.log ('linked js'); 

$(document).ready(onReady);

function onReady() {
    clickHandlers();
}

function clickHandlers() {
    // $('#submitBtn').on('click', handleSubmit);
    // $(document).on('click', '.deleteButton', deleteBook);
    // $(document).on('click', '.isRead', updateisRead);
}